export default function aliveNeighbors(neighbors) {
  let alive = 0;

  for (let i = 0; i < neighbors.length; i++) {
    if (i === true) {
      alive = alive + 1;
    }
    // return neighbors[x];
  }

  return alive;
}
